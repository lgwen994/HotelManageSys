@model IEnumerable<HotelManageSys.Models.Parking>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParkingNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAvailable)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParkingNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAvailable)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ParkingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ParkingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ParkingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
